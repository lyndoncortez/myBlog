<h1>Articles</h1>

<table>
  <tbody>
    <% @articles.each do |article| %>
      <tr>
        <td><%= article.title %></td>
        <td><%= link_to 'Show', article.title %></td>
      </tr>
   <% end %> 
  </tbody>
</table>

